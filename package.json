{
  "name": "cozybox",
  "version": "0.1.0",
  "private": true,
  "description": "CozyBox by Fossey's Distillery — website backed by Square APIs",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c magenta,cyan \"cd frontend && PORT=3001 npm start\" \"cd backend && uvicorn server:app --reload --port 4040\"",
    "frontend": "cd frontend && PORT=3001 npm start",
    "backend": "cd backend && uvicorn server:app --reload --port 4040"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
